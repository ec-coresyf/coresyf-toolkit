#PASSING EXECUTION
#Passing execution of the designated folder containing the .xlxs files (where mean vectors and covariance matrices are recorded), and the output path), no parameters are needed as the script harvests everything it needs from the .xlxs files themselves.
./run --Ssource test_data/inputs.zip --Ttarget1 myoutput1 --Ttarget2 myoutput_details